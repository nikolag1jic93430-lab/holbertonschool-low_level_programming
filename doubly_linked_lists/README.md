Doubly linked lists

* A doubly linked list is a chain of nodes, each with prev + next.
* Easier insertions/deletions because you can go backward.
* Whenever you insert or remove a node, always fix both pointers.
* Traversal is possible in both directions.
